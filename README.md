Creating a miniconda environment, from the top level directory

`conda env create -f environment.yml`

Activate the new environment `conda activate ecd-ultralytics`

Verify its installed correctly `conda env list`

Install ultralytics seperately per their recomendation

`conda install -c conda-forge ultralytics`
